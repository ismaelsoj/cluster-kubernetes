[workflow]

persistent_facts = [
  "Todo fluxo de quick-dev deve obrigatoriamente gerar uma especificação no padrão BMad method. Independente da ferramenta ou modelo de LLM escolhido, as especificações não devem ser armazenadas nos diretórios de cada ferramenta, e sim seguir o padrão do BMad de diretório de especificações.",
  "A especificação deve ser criada na primeira chamada e exige aprovação do usuário. Após a aprovação, o agente pode iniciar a implementação imediatamente ou sugerir uma nova chamada em contexto limpo. Caso a chamada inicial já forneça uma especificação no padrão BMad, o início do desenvolvimento está automaticamente autorizado.",
  "Todo code review deve ser propositivo e registrar todos os apontamentos na especificação da história que está sendo analisada. As alterações podem ser sugeridas, mas só aplique se for expressamente solicitado. Quando essas alterações forem aplicadas, seja via fluxo de code review ou de desenvolvimento (analisando os apontamentos da especificação original), deve-se atualizar o documento marcando cada item concluído."
]
