[workflow]

persistent_facts = [
  "CRITICAL REQUIREMENT [GIT WORKFLOW]: Antes de iniciar qualquer desenvolvimento, execute obrigatoriamente `git pull origin main`. Em seguida, crie e mude para uma nova branch a partir da `main` com nome descritivo (baseado na especificação ou prefixo da história). Commits diretos na branch `main` são ESTRITAMENTE PROIBIDOS.",
  "CRITICAL REQUIREMENT [GIT PERMISSIONS]: A execução autônoma de `git add` ou `git commit` é PROIBIDA. Você DEVE solicitar e obter autorização explícita do usuário ANTES de executar esses comandos."
]
