# Kustomization base - network-policies - cluster-kubernetes
# Políticas East-West globais (sem overlays por ambiente — políticas são invariantes)
# Recursos (isolation-policies.yaml) serão adicionados no Épico 2
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources: []
