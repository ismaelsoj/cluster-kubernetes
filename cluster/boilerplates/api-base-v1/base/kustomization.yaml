# Kustomization base - api-base-v1 - cluster-kubernetes
# Boilerplate de API — template reutilizável para publicação de APIs na plataforma
# Recursos serão adicionados na Story 4.1
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources: []
